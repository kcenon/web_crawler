// Package middleware provides the middleware chain for request/response processing.
//
// It implements a composable pipeline pattern for retry logic,
// rate limiting, robots.txt compliance, proxy rotation, and authentication.
package middleware
